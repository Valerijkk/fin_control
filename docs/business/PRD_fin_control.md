# 📋 PRD: FinControl — учебная инвестиционная платформа и обменник

> 📌 **Единая терминология:** во всём проекте под FinControl понимается **одно приложение** (мобильное на Flutter; веб-сборка опциональна для практик по сниффу трафика), используемое для всех 10 практик.

---

## 🎯 1. Цель продукта

**FinControl** — учебный pet-project для тестировщиков: одно и то же приложение (мобильное и при необходимости веб) на Flutter, объединяющее:

| Модуль | Описание |
|--------|----------|
| 💰 **Учёт расходов/доходов** | Текущий функционал — запись, фильтрация, статистика |
| 💱 **Обменник валют** | Калькулятор обмена по актуальным курсам, история операций |
| 📈 **Мини-инвестиции** | Виртуальный портфель (покупка/продажа «активов» по курсам валют), без реальных денег |

Все сетевые запросы (курсы валют, при необходимости — свой API) проходят по HTTP/HTTPS, чтобы на базе этого же приложения отрабатывать практики: **Charles**, **Proxyman**, **Android Studio**, **Xcode**, **ADB**, **Sentry**, **AppMetrica**, **Firebase**, **TestFlight**, **Android-дистрибуция**.

---

## 👤 2. Роли пользователей

| Роль | Описание |
|------|----------|
| **Ученик (тестировщик)** | Единственная роль: использует **одно и то же приложение** FinControl для учёта расходов/доходов, обменника и мини-портфеля, параллельно отрабатывая все 10 практик |

---

## ⚙️ 3. Функциональные требования

### 3.1 💰 Учёт расходов/доходов (существующее)

- Запись расходов/доходов: сумма, название, категория, дата, фото чека
- Категории: встроенные + пользовательские (персист в `shared_preferences`)
- Список с фильтрами (категория, период, поиск), свайп-удаление, UNDO
- Статистика по категориям и периодам
- Тёмная тема (Material 3)
- Локальное хранение: SQLite (`sqflite`)

### 3.2 💱 Обменник валют

- Экран «Обменник»: выбор валюты «из» и «в», ввод суммы, расчёт по актуальному курсу
- Курсы загружаются с публичных API (`exchangerate.host`, `open.er-api.com` — уже есть в проекте), кэш в `shared_preferences`
- Отображение курсов на экране (RUB/USD/EUR и при необходимости другие пары)
- **История операций обмена** (локально в SQLite): дата, сумма из, валюта из, сумма в, валюта в, курс на момент операции — для практики тестирования данных и сценариев

### 3.3 📈 Мини-инвестиции

- Экран «Портфель»: виртуальный баланс в выбранной валюте (RUB/USD/EUR), список «активов»
- «Активы» — упрощённо: валютные пары (например «USD», «EUR»). Покупка/продажа по текущему курсу из API
- Локальное хранение портфеля и сделок в SQLite (без реальных денег)
- Отображение текущей стоимости портфеля и истории сделок

### 3.4 🔧 Настройки и инфраструктура

- Настройки: тема, базовая валюта для отображения, очистка кэша курсов
- Все запросы к API курсов — по HTTPS с явным `User-Agent` и заголовками, чтобы трафик был виден в Charles/Proxyman при настройке прокси на устройстве/эмуляторе

---

## 📐 4. Нефункциональные требования

### 4.1 🖥️ Платформы

| Платформа | Требования |
|-----------|------------|
| **Android** | Минимум API 21, поддержка эмулятора и реального устройства |
| **iOS** | Поддержка симулятора и реального устройства (после добавления `ios/`) |
| **Web** *(опционально)* | Для практики сниффа в браузере через Charles/Proxyman |

### 4.2 🛠️ Инструменты для практик тестировщиков

Одно и то же приложение FinControl (мобильное и при необходимости веб) позволяет отрабатывать **10 практик**:

| # | Практика | Что отрабатывается | Файл |
|---|----------|--------------------|------|
| 01 | **Charles** | Перехват HTTP/HTTPS трафика (с доверенным сертификатом) | `01-charles.md` |
| 02 | **Proxyman** | Снифф трафика на Android/iOS | `02-proxyman.md` |
| 03 | **Android Studio** | Установка в эмуляторе, настройка logcat | `03-android-studio.md` |
| 04 | **Xcode** | Установка в симуляторе iOS, логи в Console | `04-xcode.md` |
| 05 | **ADB** | Установка, логи, скриншоты, очистка данных | `05-adb.md` |
| 06 | **Sentry** | Подключение SDK, тестовые события и краши | `06-sentry.md` |
| 07 | **AppMetrica** | Заведение приложения, сессии и устройства | `07-appmetrica.md` |
| 08 | **TestFlight** | Сборка и публикация iOS-приложения | `08-testflight.md` |
| 09 | **Android-дистрибуция** | Сборка, подпись, Google Play Internal / Firebase App Distribution | `09-android-distribution.md` |
| 10 | **Firebase** | Crashlytics, FCM, Analytics, Remote Config, Performance, In-App Messaging | `10-firebase.md` |

### 4.3 🎨 UX-критерии

- **Понятные подписи:** кнопки и поля имеют однозначные подписи (например «Сохранить», «Обменять», «Сумма»); диалоги подтверждения — ясные формулировки («Удалить запись?», «Подтвердите»)
- **Обратная связь при действиях:** после сохранения, удаления или операции — SnackBar или возврат на список с обновлёнными данными; при загрузке данных с сети — индикатор или состояние «офлайн»/ошибка с возможностью повторить
- **Единообразие:** основные действия — одна и та же кнопка (`FilledButton`/`PrimaryButton`); настройки доступны с каждого экрана через иконку в AppBar; пустые состояния — единый стиль сообщений («Нет записей», «Пока нет операций»)

> 💡 Детали по экранам (кнопки, навигация, ошибки, загрузка) — в [docs/testing/features](../testing/features/) (раздел «Ожидаемое поведение UI» в каждом файле фичи).

### 4.4 📚 Документация для учеников

Практики (пошаговые инструкции) по каждой технологии: что настроить, что проверить, какие сценарии тестировать на базе **одного и того же приложения** FinControl. Практики размещаются в `docs/practices`.

---

## ⚠️ 5. Ограничения и допущения

- Нет бэкенда в репозитории: курсы валют — только публичные API; портфель и обмен — локально в SQLite
- При необходимости демо «своего API» для сниффа можно добавить один минимальный сервер (например Node/Express или Dart shelf) с одним эндпоинтом — по решению команды
- Регистрация/авторизация не требуются (однопользовательское локальное приложение)
- Практики по Firebase/AppMetrica/Sentry предполагают наличие аккаунтов; в документации указано, что нужны тестовые проекты

---

## ✅ 6. Критерии готовности

> 📌 Во всех пунктах речь о **одном приложении** FinControl (мобильное; веб — опционально).

| # | Критерий | Описание |
|---|----------|----------|
| 1 | **Сборка и запуск из коробки** | Приложение собирается и запускается после клонирования и выполнения шагов из `docs/practices/00-getting-started.md` (Flutter, `flutter pub get`, эмулятор/симулятор). Без ключей доступны учёт расходов, обменник, портфель и все практики по трафику и инструментам (Charles, Proxyman, Android Studio, Xcode, ADB). |
| 2 | **Интеграции — только токены** | Sentry и AppMetrica — единственное место: `lib/config/student_env.dart` (константы `sentryDsn`, `appMetricaApiKey`); при пустых значениях приложение работает без этих сервисов. Firebase — по инструкции `docs/practices/10-firebase.md`: добавление своих конфигов `google-services.json` и `GoogleService-Info.plist`. |
| 3 | **Функциональность** | Учёт расходов/доходов, обменник и мини-портфель работают на Android и iOS (и при необходимости на web). |
| 4 | **Трафик и снифф** | Запросы к API курсов идут по HTTPS; трафик виден в Charles и Proxyman при корректной настройке прокси. |
| 5 | **Практики** | В репозитории `docs/practices` — 10 практик: Charles, Proxyman, Android Studio, Xcode, ADB, Sentry, AppMetrica, TestFlight, Android-дистрибуция, Firebase (Crashlytics, FCM, Analytics, Remote Config, Performance, In-App Messaging). |
| 6 | **README** | Описание учебной цели продукта, ссылки на практики, требования для запуска (Flutter, эмуляторы/симуляторы). |
