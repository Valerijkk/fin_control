# Перед началом любых практик

Клонирование репозитория, первый запуск, открытие проекта в Android Studio. Выполни эти шаги до практик 01–15. **Одно приложение** FinControl — всё из коробки: после этих шагов приложение уже работает без вставки ключей; ключи Sentry/AppMetrica понадобятся только в практиках 06–07 ([STUDENT_ENV.md](../STUDENT_ENV.md)), Firebase — в практиках 10–15 ([00-firebase-setup.md](00-firebase-setup.md)).

---

## Цель

Подготовить рабочее окружение: склонировать репозиторий FinControl, установить зависимости, запустить приложение на устройстве или эмуляторе и открыть проект в Android Studio так, чтобы IDE корректно распознавала Flutter-проект.

## Ожидаемый результат

- Репозиторий лежит на диске, из корня проекта выполняются `flutter pub get` и `flutter run`.
- Приложение **запускается из коробки**: приветственный экран «FinControl» → кнопка «Начать» → главный экран с навигацией (Список, Обменник, Акции, Портфель, Статистика). Никаких токенов для старта не нужно.
- В Android Studio открыта **корневая** папка проекта; в дереве видны `lib/`, `android/`, `ios/`, `pubspec.yaml`; нет массовых ошибок Gradle.

---

## 1. Клонирование и первый запуск

**Что понадобится:** установленные Git и Flutter (проверка — команды `git --version` и `flutter --version` в терминале).

### Шаг 1. Открой терминал

- **Windows:** PowerShell (Win + X → Windows PowerShell) или cmd. Можно также встроенный терминал в VS Code (Ctrl+`).
- **Mac/Linux:** Terminal или встроенный терминал в IDE.

### Шаг 2. Перейди в папку для проектов

- Пример: `cd Desktop` или `cd C:\Users\ИМЯ\Desktop`, или `cd ~/Projects`.
- Убедись, что ты не внутри другого репозитория (не должно быть скрытой папки `.git` в текущей директории, если хочешь положить fin_control рядом с другими проектами).

### Шаг 3. Клонируй репозиторий

```bash
git clone https://github.com/Valerijkk/fin_control.git
cd fin_control
```

- Если `git clone` выдаёт ошибку «command not found» — установи Git с [git-scm.com](https://git-scm.com/).
- Если ошибка доступа/таймаут — проверь интернет и при необходимости настрой прокси для Git.

### Шаг 4. Проверь Flutter и установи зависимости

```bash
flutter doctor
```

- Смотри вывод: для практик достаточно, чтобы был отмечен галочкой **Android** (или iOS на Mac). Красные крестики по Android Toolchain или Flutter нужно устранить (установить SDK, принять лицензии: `flutter doctor --android-licenses`).

```bash
flutter pub get
```

- Должно завершиться без ошибок. Это подтягивает пакеты из `pubspec.yaml`.

### Шаг 5. Запусти приложение

```bash
flutter run
```

- Если устройств несколько — в терминале появится нумерованный список (например `[1] Pixel 9 API 34`, `[2] Chrome`). Введи номер нужного и Enter. Эмулятор нужно заранее запустить из Android Studio (Tools → Device Manager → Run).
- Первый запуск может занять 1–3 минуты (сборка Gradle, компиляция).
- **Что должно получиться:** на экране приложения приветственный экран с заголовком «FinControl» и кнопкой «Начать». После нажатия «Начать» открывается главный экран с нижней навигацией: **Список**, **Обменник**, **Акции**, **Портфель**, **Статистика**.

### Проверка

- [ ] В терминале из папки `fin_control` команды `flutter doctor` и `flutter pub get` выполняются без ошибок.
- [ ] `flutter run` предлагает выбрать устройство; после выбора приложение собирается и на экране появляется приветствие «FinControl» и кнопка «Начать».
- [ ] После нажатия «Начать» отображается главный экран с нижней навигацией: Список, Обменник, Акции, Портфель, Статистика.

### Траблшутинг

Типичные причины и решения — в разделе **«Запуск и сборка»** в [FAQ](../FAQ.md): ошибка «flutter.sdk not set», Gradle sync failed, эмулятор не в списке устройств, «No pubspec.yaml found». Кратко: проверь `flutter doctor`, что команды выполняются из **корня** проекта (не из `android/`), при необходимости выполни `flutter clean` и снова `flutter pub get`.

---

## 2. Как открывать проект в Android Studio (обязательно)

**Почему это важно:** в Android Studio нужно открывать **корневую папку проекта** — ту, где лежат файл `pubspec.yaml` и папки `lib/`, `android/`, `ios/`, `docs/`. **Не** открывай папку `android/` саму по себе: тогда IDE считает проект обычным Android-приложением, не видит Flutter/Dart, и появляются сотни ошибок Gradle и красные подчёркивания по всему коду.

### Пошагово

1. Запусти Android Studio. Если уже открыт другой проект — **File → Close Project** (вернёшься на экран приветствия).
2. На экране приветствия нажми **Open** (или **File → Open**).
3. В диалоге выбора папки укажи **папку fin_control** — именно ту, внутри которой лежат `pubspec.yaml`, `lib`, `android`, `ios`. Не выбирай папку `android` внутри неё.
4. Нажми **OK**. Дождись окончания индексации (внизу статус «Indexing…») и при необходимости **Gradle sync** (может предложить сам или через **File → Sync Project with Gradle Files**).
5. **File → Settings** (на Mac: **Android Studio → Settings**) → **Plugins**. Убедись, что установлены и включены **Flutter** и **Dart**. Если их нет — вкладка **Marketplace**, поиск «Flutter», установка, затем перезапуск Android Studio.

### Проверка

- [ ] В дереве проекта (Project) видны папки `android`, `ios`, `lib`, `test`, файл `pubspec.yaml` в корне.
- [ ] При открытии файлов из `lib/` подсветка синтаксиса Dart работает (нет массовых красных подчёркиваний из-за «не Flutter-проекта»).

Подробнее и решение проблем: [ANDROID_STUDIO_LAUNCH.md](../technical/ANDROID_STUDIO_LAUNCH.md). Частые ошибки при открытии не той папки — в [FAQ](../FAQ.md#в-android-studio-сотни-ошибок-красные-подчёркивания-по-всему-проекту-что-не-так).

---

## Дальше

- **Порядок практик:** [README — список практик](README.md#порядок-прохождения).
- **Пошаговые инструкции по каждой практике:** [01-charles](01-charles.md), [02-proxyman](02-proxyman.md), [03-android-studio](03-android-studio.md) и далее по списку.
- **Сводный пошаговый гайд (оглавление):** [practices-step-by-step.md](../practices-step-by-step.md).
- **Ключи Sentry/AppMetrica (практики 06–07):** [STUDENT_ENV.md](../STUDENT_ENV.md) → `lib/config/student_env.dart`.
- **Firebase (практики 10–15):** [00-firebase-setup.md](00-firebase-setup.md); критерии приёмки — [acceptance-criteria/README.md](../acceptance-criteria/README.md).
- **FAQ и типичные ошибки:** [FAQ.md](../FAQ.md).
